{
  "name": "todo-list-api",
  "version": "1.0.0",
  "description": "Simple RESTful API for To-Do List",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build": "mkdir -p dist && cp -r public/* dist/"
  },
  "dependencies": {
    "express": "^4.17.1",
    "uuid": "^8.3.2",
    "@netlify/functions": "^2.0.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.12"
  }
}
